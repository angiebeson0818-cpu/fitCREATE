
# fitCREATE Pro - Virtual Style Engine

The ultimate AI fashion platform. Upload a photo, try on any outfit, retouch your face, and sculpt your body.

## 🚀 How to Run Locally

1. **Install Node.js**: Ensure you have Node.js installed.
2. **Setup Folder**: Create a folder and put all these files inside.
3. **Environment Variable**: Create a `.env` file and add:
   ```env
   API_KEY=YOUR_GEMINI_API_KEY_HERE
   ```
4. **Install Dependencies**:
   ```bash
   npm install
   ```
5. **Start**:
   ```bash
   npm run dev
   ```

## 📦 How to Upload to GitHub

1. Create a new repository on GitHub.com.
2. Run these commands in your folder:
   ```bash
   git init
   git add .
   git commit -m "fitCREATE Pro: AI Features & Subscription"
   git remote add origin https://github.com/yourusername/fitcreate.git
   git push -u origin main
   ```

## 🌍 How to Deploy (Get a Link)

1. Connect your GitHub to **Vercel** or **Netlify**.
2. Select this project.
3. Add your `API_KEY` in the "Environment Variables" setting.
4. Deploy! You'll get a public link to share.
